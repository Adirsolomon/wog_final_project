apiVersion: apps/v1
kind: Deployment
metadata:
  name: memory-game-deployment
  namespace: wog-namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: memory-game
  template:
    metadata:
      labels:
        app: memory-game
    spec:
      containers:
      - name: memory-game
        image: adiros/wog_final-memory_game:latest
        ports:
        - containerPort: 8000
        volumeMounts:
        - mountPath: /app
          name: memory-game-volume
        command: ["sh", "-c", "python manage.py runserver 0.0.0.0:8000"]
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
          requests:
            memory: "256Mi"
            cpu: "250m"
      volumes:
      - name: memory-game-volume
        persistentVolumeClaim:
          claimName: wog-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: memory-game-service
  namespace: wog-namespace
spec:
  selector:
    app: memory-game
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP